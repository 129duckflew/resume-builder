apiVersion: v1
kind: Service
metadata:
  name: pdf-service
  namespace: resume-builder
  labels:
    app: resume-pdf-service
spec:
  # 纯集群内网服务：不进 Ingress，只对 backend 暴露。
  type: ClusterIP
  selector:
    app: resume-pdf-service
  ports:
    - port: 8090
      targetPort: 8090
