apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: resume-builder
  labels:
    app: resume-backend
spec:
  selector:
    app: resume-backend
  ports:
    - port: 8080
      targetPort: 8080
