apiVersion: apps/v1
kind: Deployment
metadata:
  name: resume-backend
  namespace: resume-builder
  labels:
    app: resume-backend
spec:
  # 不写 replicas：由 KEDA (HTTPScaledObject) 管理副本数，
  # 避免 ArgoCD 与 KEDA 互相拉扯导致启动中的 Pod 被杀。
  selector:
    matchLabels:
      app: resume-backend
  template:
    metadata:
      labels:
        app: resume-backend
      annotations:
        vault.hashicorp.com/agent-inject: "true"
        vault.hashicorp.com/role: "resume-builder"
        vault.hashicorp.com/agent-inject-secret-db-creds: "secret/data/resume-builder"
        vault.hashicorp.com/agent-inject-template-db-creds: |
          {{- with secret "secret/data/resume-builder" -}}
          export DB_USER="{{ .Data.data.DB_USER }}"
          export DB_PASS="{{ .Data.data.DB_PASS }}"
          export JWT_SECRET="{{ .Data.data.JWT_SECRET }}"
          {{- end }}
    spec:
      # host.docker.internal 走 8.8.8.8 解析会被宿主机代理 fake-ip 污染，
      # 直接钉到 lima 宿主机网关（等同 Mac 的 localhost），绕过 DNS。
      hostAliases:
        - ip: "192.168.5.2"
          hostnames:
            - host.docker.internal
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: backend
          image: ghcr.io/129duckflew/resume-builder/backend:3113a78ef3b620aad1ab70f706ca233110f09177
          imagePullPolicy: IfNotPresent
          command: ["sh", "-c"]
          args:
            - |
              . /vault/secrets/db-creds
              exec java -jar app.jar
          envFrom:
            - configMapRef:
                name: app-config
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 1Gi
          # Spring Boot 冷启动约需 100-120s（受限 CPU 下）。
          # startupProbe 成功前，liveness/readiness 均不生效，避免启动期被误杀。
          startupProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            periodSeconds: 10
            failureThreshold: 30 # 最长容忍 300s 启动时间
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            periodSeconds: 5
