apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: resume-builder
  labels:
    app: resume-frontend
spec:
  selector:
    app: resume-frontend
  ports:
    - port: 80
      targetPort: 80
